apiVersion: v1
kind: Service
metadata:
  name: {{ include "signUp.fullname" . }}
  labels:
    {{- include "signUp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "signUp.selectorLabels" . | nindent 4 }}
